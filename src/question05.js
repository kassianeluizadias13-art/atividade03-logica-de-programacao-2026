import leia from "readline-sync"

var totalGols = gols * partidas
 
var partidas = leia.questionInt("Quantas partidas analisadas: ")
 for (var contador = 1; contador <= partidas; contador++) {
        var gols = leia.questionInt("Total de gol a cada partida" + contador + ": ")
 }



 console.log(totalGols)